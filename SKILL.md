---
name: cumcm-paper-hand-skill
description: 数学建模竞赛(CUMCM 国赛 / MCM/ICM 美赛)建模分析与论文写作技能。在参赛者整理好的参赛文件夹(题目/、问题X/建模笔记.md、代码/、结果/)中完成建模、求解与论文撰写,最终以 LaTeX 交付(论文.tex + 编译 PDF)。Use when 用户提到"数学建模"、"建模比赛"、"数模论文"、"数学建模竞赛"、"建模分析"、"建模求解"、"写数模论文"、"帮我做这道建模题"、"帮我写论文"、"优秀论文参考"、"论文写作指导"或类似表述。
---

# 数学建模竞赛论文写作技能

面向 CUMCM / MCM/ICM 参赛者。在参赛者的参赛文件夹内按三步工作:**摸底 → 建模求解 → 写论文(LaTeX 交付)**。

## When to Use / When NOT to Use

**Use when**:用户要在参赛文件夹中建模、求解或撰写数模论文,或要求参考获奖论文(本技能 [ref/](ref/) 目录内置)学习论文写法。

**NOT use when**:
- 用户只需要算法概念解释或通用数学知识问答(不涉及参赛文件夹与论文产出);
- 用户明确指定使用其他建模 skill(与本技能并列的通用 math-modeling 类技能)。

## 1. 参赛文件夹结构(工作前提)

参赛者应已按以下结构整理文件夹;未整理时先按此结构补齐,或按现有文件归拢后继续:

```
my-mcm-project/
├── 题目/                      # 原始题目与附件(只读,不得修改)
├── 问题A/
│   ├── 建模笔记.md            # 本题建模全过程(模型、公式、参数、验证)
│   ├── 代码/                  # 本题求解代码(可运行)
│   ├── 结果/                  # 本题产出的图、表、数据
│   └── 解答.md                # 本题解答草稿(可选)
├── 问题B/                     # 同上
├── 论文/                      # 论文输出:论文.tex、论文.pdf、附图/
└── 参考资料/
```

[模板目录](templates/) 与 [参考论文](ref/) 位于本技能目录下,与 SKILL.md 同级;找不到时直接问参赛者。

## 2. 工作流程

### Step 1 摸底(动笔前必须做)

1. 读取 `README.md` 与文件夹结构,列出全部题目、数据、已有笔记。
2. 读取 `题目/` 原始题目,逐问标注:研究对象、已知条件、核心矛盾、每问任务。
3. 检查每题 `建模笔记.md`;缺失的先补齐再继续。
4. 浏览 [ref/](ref/) 中与本题类型相近的 1–2 篇获奖论文(规则见第 4 节)。

**出口标准**:题目逐问理解完毕,每题有建模笔记(或补齐方案),已选定参考论文。

### Step 2 建模与求解(逐题推进)

对每题执行闭环,并把每一步写进该题 `建模笔记.md`:

> 问题分析 → 模型建立 → 模型求解 → 结果分析 → 小结

1. **问题分析**:题目要求 → 关键难点 → 数据/变量特征 → 总体建模思路 → 求解与检验方法。必须解释"为什么该模型适合本题"(选回归要说明变量间的相关趋势;选聚类要说明为何对样本分类;选优化要指出目标函数、决策变量、约束)。
2. **模型建立**:先文字说明思路,再给公式,再逐项解释公式含义;优化问题给出标准型。
3. **模型求解**:用 `问题X/代码/` 中已有代码或新写代码求解,记录关键参数与算法;结果写入 `结果/` 并注明来源文件。
4. **结果分析**:结果趋势是否符合实际、误差是否合理、是否做敏感性/交叉验证/多模型对比等检验。
5. **小结**:本问用了什么模型、求得什么结果、指标多少、给下一问留下什么输入。

**出口标准**:每题笔记闭环齐全,结果文件可追溯(有来源),下一问的输入已明确。

### Step 3 撰写论文(LaTeX 交付)

1. 通读全部建模笔记与结果,先梳理**叙事逻辑**:问题二是否使用问题一的模型?问题三是在问题一基础上扩展还是新模型?前一问的输出是否成为后一问的输入?后一问能否验证前一问的合理性?——先梳理再动笔。
2. 读取 [论文模板.tex](templates/论文模板.tex),在其骨架上写作,输出到 `论文/论文.tex`。骨架固定为:

```
摘要 → 问题重述 → 问题分析 → 模型假设 → 符号说明 → 模型建立与求解 → 模型评价与推广 → 参考文献 → 附录
```

3. **摘要最后写**(正文定稿后写),每问句式:针对…问题,首先…,随后建立…模型,采用…算法求解,得到…结果;通过…检验,表明模型…。直接写数值与指标,禁止"结果良好、具有一定可行性"之类空话。
4. 每问末尾加小结:"综上,本文建立了…模型,求得…,模型在验证集上的指标为…,说明模型…,所得结果将在问题X作为…使用"。
5. 图表:图/表编号+标题,三线表(booktabs),对比表中最优值加粗 `\textbf{}`,每张图表必须在正文中被引用与讨论("看图说话")。
6. 用 XeLaTeX 编译 `论文.tex` 两次(处理交叉引用),产出 `论文.pdf`;编译报错必须修复到零错误。
7. 若参赛者已提供 `论文/` 下草稿,在草稿基础上修改,不推倒重来。
8. 对照第 3 节检查清单逐项核对,把核对结果汇报给参赛者。

**出口标准**:`论文.tex` 编译零错误,`论文.pdf` 交付,检查清单全部通过或已知偏差已说明。

## 3. 写作规范(强制,逐条执行)

### 3.1 问题重述
- 逐小问单独叙事;用自己的话复述,不照抄题目原文。
- 把小问整理为明确的数学任务(研究对象、已知条件、核心矛盾、任务)。

### 3.2 问题分析
- 逐小问:题目要求 → 关键难点 → 数据/变量特征 → 总体建模思路 → 求解与检验方法。
- 不提前展开公式;必须解释"为什么该模型适用",不是"我们建立某某模型"。
- 让读者知道:处理什么数据、分析什么关系、建什么模型、用什么求解、用什么指标评价。

### 3.3 模型假设
- 5–10 条;每条解释"为什么可以这样假设";末尾加过渡句"以上假设的合理性将在模型检验部分进一步验证"。
- 假设后文必须能找到对应的模型依据。

### 3.4 符号说明
- 三线表:符号 | 含义 | 单位;全文符号统一;优先学科惯例符号($\theta$、$\lambda$、$\rho$)。

### 3.5 模型建立与求解(论文核心,篇幅最大)
- **每问结构**:问题目标 → 数据处理 → 变量定义 → 模型原理 → 模型建立 → 参数确认 → 算法求解 → 结果呈现 → 结果检验 → 本问结论。
- **公式不得直接给出**:公式前有引导文字("由…定律可得""考虑到…因素,引入…"),公式中阐明每个变量含义,公式后说明能算什么、下一步怎么用。三段式:先文字 → 公式 → 解释。
- 关键推导放正文,冗长推导放附录,正文注明"详细推导见附录A"。
- 优化问题给出标准型;不写"我们调用了 xxx 函数"这类代码说明书式表述。
- **算法**:用伪代码或有效流程图(必须体现初始化、迭代更新、适应度计算、约束判断、终止条件等真实步骤,禁止"输入→建模→运行→输出"式流程图)。
- **数据预处理**:交代原始数据存在什么问题、用什么方法、关键参数、为何这样选、处理后保留什么特征、是否展示处理效果。
- **证明结果可信**:解释结果趋势、是否符合现实背景、不同方法结果是否一致、误差是否合理、对参数是否敏感;检验方法:误差分析/残差分析/交叉验证/敏感性分析/多模型对比/与已知结论比较。正文必须直接回答题目。
- **模型建立前的可选叙事**:数据趋势先行分析(说明为何如此建模)、背景必要性叙事。

### 3.6 模型评价与推广
- 四维度:优点 / 缺点(真实具体,如"计算复杂度高,大规模下求解超过 X 分钟")/ 改进方向 / 推广(结合本题背景,不空谈)。

### 3.7 参考文献
- 只列正文真正引用的文献,按引用顺序编号,正文标注 [1][2];格式统一。
- **引用必须是真实文献,禁止编造**;格式参考 [ref/](ref/) 获奖论文的参考文献列表。

### 3.8 附录
- 放冗长推导、核心源码、大表格、原始数据展示;编号+标题,正文注明"详见附录A"。

## 4. ref/ 获奖论文使用规则

- [ref/*.md](ref/) 为 OCR 可读版(公式/上下标可能有误差),原 [ref/*.pdf](ref/) 为准。
- 写论文前浏览与本题类型相近的 1–2 篇(优化题→A196;B题物理→B060/B157;C题数据→C023/C132),学其摘要写法、章节结构、图表设计、结果呈现。
- **只学不抄**:学习结构与表达方式,禁止复制原文内容或公式;借鉴思路须结合本题重新表述。
- 需要精确公式/数据时对照原 PDF 或询问参赛者。

## 5. 硬性约束

1. **不编造**:论文中每个数值必须能追溯到 `结果/` 或 `代码/` 中的某个文件;找不到来源的结果禁止写入论文。
2. **不清楚就问**:建模笔记缺失、结果对不上、附件打不开时,先问参赛者,不擅自假设。
3. **先草稿后终稿**:先出 Markdown 草稿供确认,再落 LaTeX 终稿与 PDF。
4. **语言**:国赛中文;美赛中文稿定稿后译英文,并检查英文表达。
5. 每问必须形成"分析→建模→求解→结果分析→小结"闭环;子问题间必须有承接过渡,前问输出成为后问输入。

## 6. 交付检查清单(写完逐项核对)

- [ ] 骨架完整:摘要 → 问题重述 → 问题分析 → 模型假设 → 符号说明 → 模型建立与求解 → 模型评价与推广 → 参考文献 → 附录
- [ ] 每问闭环,子问题有承接过渡
- [ ] 摘要最后写,每问有具体数值与指标,无空话
- [ ] 问题重述用自己话;问题分析解释了模型适用性
- [ ] 假设 5–10 条且有理由;符号三线表且全文统一
- [ ] 公式有引导文字、逐项解释;优化问题有标准型
- [ ] 每张图表有编号标题、正文被引用讨论、对比表最优值加粗
- [ ] 数据预处理五要素齐全;结果有检验;正文直接回答题目
- [ ] 参考文献真实、格式统一、正文有标注
- [ ] `论文.tex` 用 XeLaTeX 编译零错误,交付 `论文.pdf`
