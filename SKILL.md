---
name: cumcm-paper-hand-skill
description: 本技能应在用户要求"数学建模"、"建模比赛"、"数模论文"、"数学建模竞赛"、"建模分析"、"建模求解"、"写数模论文"、"论文写作指导"、"优秀论文参考"或提及数学建模相关任务时使用。适用于全国大学生数学建模竞赛(CUMCM)、美国大学生数学建模竞赛(MCM/ICM)等各类比赛。技能在参赛者已整理好的参赛文件夹(原始题目、每题建模笔记、求解代码与结果)中工作,以论文写作为核心,提供论文写作规范与模板,并参考 ref/ 目录下的获奖优秀论文。
---

# 数模 SKILL —— 数学建模竞赛论文写作与全流程辅助

## 一、本技能是什么

本技能面向数学建模竞赛参赛者(国赛 CUMCM / 美赛 MCM/ICM)。它的核心定位:

1. **在你的参赛文件夹里工作**——你只需要把原始题目、每道题的建模笔记、求解代码、结果图表整理成规范的文件夹,AI Agent 就能帮你完成从分析到论文的全部工作。
2. **以论文写作为核心产出**——本技能包含一套经过实战打磨的论文写作规范(骨架、逐节写法、过渡衔接、结果呈现、摘要技法),确保论文结构完整、逻辑闭环、符合评阅口味。
3. **参考获奖论文**——`ref/` 目录内置多篇获奖优秀论文(含 OCR 可读版),AI Agent 写论文时学习其结构、表达与图表方式,但不照抄。

---

## 二、工作模式:你的文件夹怎么组织(重要)

> 参赛者需要把**原始题目**、**每个题目的建模和解答**分文件夹整理好,本技能才能高效工作。不整理也能做,但效果打折。

### 2.1 推荐的参赛文件夹结构

```
my-mcm-project/                      # ← 一次参赛,一个文件夹
├── README.md                        # 项目说明:题目清单、分工、进度
├── 题目/                            # 原始题目与数据
│   ├── 2024A_题目.pdf               # 原始题目原文(必须保留!)
│   └── 附件/                        # 题目附件数据
│       ├── data1.xlsx
│       └── ...
├── 问题A/                           # 每个题目一个文件夹
│   ├── 建模笔记.md                  # 本题建模:思路、模型、公式、验证
│   ├── 代码/                        # 本题求解代码(可运行)
│   │   ├── main.py
│   │   └── ...
│   ├── 结果/                        # 本题产出的图、表、数据
│   │   ├── 图1_xxx.png
│   │   └── result.csv
│   └── 解答.md                      # 本题解答草稿(论文章节素材)
├── 问题B/
│   └── ...
├── 论文/                            # 最终论文
│   ├── 论文.md                      # 论文初稿/终稿
│   └── 附图/                        # 论文用图(高清)
└── 参考资料/
    └── ...                          # 你自己找的文献
```

- 题目原文**单独放 `题目/` 且不修改**——论文的"问题重述"必须从这里来,AI Agent 会先读它。
- 每题的 `建模笔记.md` 是 AI 写论文的**第一手素材**:你在建模过程中的每一步思考都记在这里,比让 AI 从零推断可靠得多。
- `解答.md` 可以理解为"本题的完整解答",最后汇总进论文正文。

### 2.2 模板

`templates/` 目录提供了:
- `templates/论文模板.md` —— 论文写作模板(对应下方"论文整体结构骨架",含每节要点与占位符)
- `templates/论文模板.tex` —— LaTeX 版论文模板(国赛格式,含三线表、公式、算法环境)
- `templates/建模笔记模板.md` —— 每题建模笔记的模板
- `templates/目录结构模板.md` —— 上面的文件夹结构模板(可直接复制使用)
- `templates/README.md` —— 模板使用说明

AI Agent 在动笔前应读取 `templates/论文模板.md`,按模板骨架写作。

---

## 三、工作流程(AI Agent 执行)

当参赛者提出"帮我做这道题""帮我写论文"等请求时,按以下三阶段工作。**核心原则:建模笔记 → 代码结果 → 论文,信息逐级传递,不跳步、不凭空编造结果。**

### 阶段 1:摸底与准备(10 分钟)

1. 读取 `README.md` 和文件夹结构,列出所有题目、数据、已有笔记。
2. 读取 `题目/` 下的原始题目,逐问标注:研究对象、已知条件、核心矛盾、每问任务。
3. 检查每题的 `建模笔记.md` 是否齐全;若缺失,先补齐笔记再往下走(笔记可以很短,但必须覆盖:用了什么模型、为什么、公式、参数、验证方法)。
4. 若有 `ref/` 参考论文,浏览其题目与结构,确定哪几篇与本题类型相关(详见第五节)。

### 阶段 2:建模与求解(逐题推进)

对每一题按以下闭环执行,并把每一步写进 `建模笔记.md`:

> **问题分析 → 模型建立 → 模型求解 → 结果分析 → 小结**

1. **问题分析**:题目要求 → 关键难点 → 数据/变量特征 → 总体建模思路 → 求解与检验方法。写清楚"为什么这个模型适合本题"(例如选回归模型,要说明变量间存在什么相关趋势;选聚类,要说明为什么对样本分类)。
2. **模型建立**:公式不得直接给出——先文字说明思路,再给公式,再解释公式每一项含义。优化问题要给出标准型(目标函数、决策变量、约束)。
3. **模型求解**:用参赛者文件夹中的代码或新写代码求解,记录关键参数与算法;结果写入 `结果/` 并注明来源文件。
4. **结果分析**:说明结果趋势是否符合实际、误差是否合理、是否做了敏感性/交叉验证/多模型对比等检验。
5. **小结**:本问用了什么模型、求得什么结果、指标多少、给下一问留下了什么输入。

### 阶段 3:论文撰写(核心产出)

1. 通读全部建模笔记与结果,梳理**叙事逻辑**:问题二是否用了问题一的模型?问题三是在问题一基础上扩展还是新模型?前一问的输出是否成为后一问的输入?后一问能否反过来验证前一问的合理性?——**这是论文最重要的东西,先梳理再动笔**。
2. 读取 `templates/论文模板.md`,按骨架写作:摘要 → 问题重述 → 问题分析 → 模型假设 → 符号说明 → 模型建立与求解 → 模型评价与推广 → 参考文献 → 附录。
3. **摘要最后写**,每问按句式:针对…问题,首先…,随后建立…模型,采用…算法求解,得到…结果;通过…检验,表明模型…。直接写数值与指标,不写"结果良好、具有一定可行性"这类空话。
4. 每问末尾加小结:"综上,本文建立了…模型,求得…,模型在验证集上的指标为…,说明模型…,所得结果将在问题X作为…使用"。
5. 图表:图/表编号+标题,三线表,对比表中最优值加粗,每张图表必须在正文中被引用和讨论("看图说话")。
6. 若参赛者提供 `论文/` 下的已写草稿,在草稿基础上修改完善,不推倒重来。
7. 写完后对照第六节检查清单自检,逐项核对。

---

## 四、论文写作指南(核心规范)

> 这一节是本技能的灵魂。每一节都有明确的目的:让评阅人在最短时间内看清你做了什么、为什么这么做、结果是否可信。

### 4.1 论文整体结构骨架

数模论文的标准骨架(按出现顺序):

```
摘要 → 问题重述 → 问题分析 → 模型假设 → 符号说明 → 模型建立与求解 → 模型评价与推广 → 参考文献 → 附录
```

> **核心逻辑链**:每个子问题都要形成"**问题分析 → 模型建立 → 模型求解 → 结果分析 → 小结**"的闭环。
>
> - **最重要:叙事逻辑**:不要出现问题重述的时候 1、2、3 问题都写了,但每一问都像是一篇独立的文章,前面的模型和后面的模型没有一点承接。比较好的方法是提前梳理:问题二是否使用了问题一的模型,问题三是在问题一的基础上扩展,还是重新建立了新的模型,前一问的输出是否有可能变为后一问的输入,后一问是否能反过来验证前一问的合理性。

### 4.2 逐节写法精要

#### 1. 问题重述 —— 展示你的理解深度

- **需要逐个小问展开说明**:每一个小问单独叙事。
- **用自己的语言复述**:不可大段照抄原题文字,把题目中的长背景压缩掉。
- **把每一个小问题整理为明确的数学任务**:不要只写"根据附件数据建立模型并进行预测",而是写"基于附件中的数据监测分析,分析各个指标与目标变量之间的关系,建立预测模型,并利用给定数据计算未来时刻的预测结果"。问题重述可以适当改变表达顺序,但要帮助读者识别:**题目的研究对象、已知条件、核心矛盾和每一问的任务**。

#### 2. 问题分析 —— 要写建模的方案

- **需要逐个小问展开说明**:每一个小问单独叙事。
- **推荐顺序**:题目要求 → 关键难点 → 数据或者变量特征 → 总体建模思路 → 求解和检验方法。
- **注意**:问题分析不需要提前展开公式,也不能只叙述"我们建立某某模型解决某某问题",而是要解释**为什么这个问题适用于该模型**。例如选择回归模型,要说明变量之间存在什么样子的相关趋势;选择聚类模型,要说明为什么对样本进行分类;选择优化模型,要指出目标函数、决策变量和主要约束分别是什么。
- **总结**:该部分需要让人知道我们的每一问准备:处理什么数据,分析什么关系,建立什么类型的模型,使用什么方法求解,通过什么指标评价结果。

#### 3. 模型假设 —— 不是简单的忽略次要因素

- 假设要**合理且必要**:每条假设都要解释"为什么可以这样假设"。
- 数量:**5–10 条**为宜。太少 → 显得考虑不周全;太多 → 模型太特殊、适用面窄。
- 假设要**覆盖关键简化**:如忽略次要因素、近似处理、理想化条件等。
- 末尾加过渡句:"以上假设的合理性将在模型检验部分进一步验证。"
- 做有承接的假设:假设一旦写出,**后文最好能够找到它对应的模型依据**。

#### 4. 符号说明 —— 只放真正重要的因素

- 使用**三线表**格式:符号 | 含义 | 单位(如有)。
- **全文符号统一**:同一符号不在不同地方表示不同含义。
- 优先使用学科惯例符号($\theta$ 角度、$\lambda$ 比率/特征值、$\rho$ 密度等)。
- 符号表放在模型建立之前,方便评阅人随时查阅。

#### 5. 模型建立与求解 —— 论文的核心(占篇幅最大)

**5.1 每个子问题的标准写法**

每个子问题都建议按以下结构组织:

> 问题目标 → 数据处理 → 变量定义 → 模型原理 → 模型建立 → 参数确认 → 算法求解 → 结果呈现 → 结果检验 → 本问结论。

**"每个子问题末尾加一个小结"** 是非常好的做法:让评阅人读完每个子问题都能立刻抓住你的贡献,而不需要翻回摘要。比如:
"综上,本文建立了…模型,求得…,模型在验证集上的指标为…,说明模型具有…,所得结果将在问题X作为…使用"

**5.2 建模过程的叙述技巧**

- **公式不得直接给出**:每个重要公式前面要有引导文字("由…定律/原理可得""考虑到…因素,引入…")。公式提出前要说明为什么要建立这个关系,公式中阐明每个变量的含义,公式后要给出这个公式能计算什么、下一步该怎么使用。
- **推导过程**:关键推导步骤放在正文,冗长的推导放附录,正文中注明"详细推导见附录A"。
- **"先文字,后公式,再解释"的三段式**:先用文字说思路 → 给出数学公式 → 解释公式每一项的含义。
- **标准性**:优化问题要给出最后优化的标准型,求解问题要写清楚求解标准型。
- **避免写成代码说明书**:要在数学层面讲清楚你在做什么,不要写"我们调用了 xxx 函数,输入了 xxx"。

**5.3 结果呈现技巧**

- **图和表是评阅人的第一关注点**:每张图/表都要做到"看图说话"——不看正文也能大致理解。
- 图/表编号 + 标题:如"图5 三种策略下的成本对比"。
- 表格优先用三线表。
- **关键数据加粗或高亮**:在对比表中,最优值加粗,让评阅人一眼看到。
- **每个图表都要在正文中被引用和讨论**:不可放一张图却不分析它说明了什么。
- **结果要与建模呼应**:分析结果时回到模型假设,说明结果是否符合预期、是否验证了模型的合理性。

**5.4 过渡与衔接**

- 子问题之间要有**承上启下的过渡句**。如:"问题二在问题一的基础上进一步考虑了…因素,因此我们在模型一的基础上做了如下改进…"
- 体现**模型之间的递进关系**(后面的模型如何在前面的基础上改进/扩展)。

**5.5 算法部分怎样写清楚**

- 一般使用伪代码,复杂算法可以采用流程图。但是流程图不能是:输入数据 → 建立模型 → 运行程序 → 输出结果。一个有效的流程图应该体现算法内部真正重要的步骤,例如初始化、迭代更新、适应度计算、约束判断和终止条件。

**5.6 数据预处理方面注意**

- 不可以直接写"首先对附件文件进行了预处理"。比较完整的数据交代至少要包括:原始的数据中存在什么问题,采用了什么方法,方法中的关键参数,参数为什么这么选,处理后保留了什么特征,是否对处理效果进行了展示。

**5.7 要证明结果可信**

- 不能直接写:"最终结果如图所示"。至少还要解释:结果呈现出什么样的趋势;是否符合现实问题背景;不同方法的结果是否一致;误差是否在合理的范围内;模型对参数的变化是否敏感。
- 常见的检验方法包括:误差分析、残差分析、交叉验证、敏感性分析、不同模型对比、与真实值或已知结论比较。
- 对极端结果进行合理性验证:不一定每问都要做,但必须根据模型选择真正适用的检验方法。
- **强调:正文必须直接回答题目**。

**5.8 写模型建立前的一些可以加的叙事**

- 统计问题是否可以根据数据趋势或者图像先对数据进行分析,从而得出为什么要如此建立模型。
- 可否先叙事背景或者其他必要性措施,让读者更容易读懂我们的建模。

#### 6. 摘要最后写

- **浓缩最具价值的信息**:用了什么模型,使用什么算法求解,得到了什么关键结果,采用了什么方法验证,最终结论如何。每一问可以按照这个句式整理:针对…问题,首先…,随后建立…模型,采用…算法求解,得到…结果;通过…检验,表明模型…。
- **不要泛泛的写结果良好、具有一定可行性**:直接写数值结果、评价指标。

#### 7. 模型评价与推广

- 从"模型的优点 / 模型的缺点 / 模型的改进方向 / 模型的应用推广"四个维度展开。
- 缺点要真实具体(如"计算复杂度高,大规模数据下求解时间超过 X 分钟"),并给出可行的改进思路。
- 推广要结合本题背景向外延伸,不说空话。

#### 8. 参考文献

- 只列正文中真正引用的文献,按引用顺序编号,正文中标注 [1][2]。
- 引用格式统一(国赛用 [1] 作者. 题名[J]. 期刊,年,卷(期):页码。 的格式;美赛按标准期刊格式)。
- 可以用 `ref/` 里优秀论文的参考文献列表作为格式参考,但**引用内容必须是真实存在的文献**,不得编造。

#### 9. 附录

- 放:冗长推导、程序源码(核心部分)、大表格、原始数据的部分展示。
- 附录要有编号与标题,正文中注明"详见附录A"。

---

## 五、参考论文(ref/)的使用规则

`ref/` 目录存放获奖优秀论文。为方便 AI Agent 通读,每篇论文都提供了 **OCR 可读版(Markdown)** 与**原 PDF**:

| 文件 | 题目 | 题型 |
|------|------|------|
| `ref/A196.pdf` / `A196.md` | 多情形下无人机烟幕遮蔽策略的建模与优化研究 | A 题(优化) |
| `ref/B060.pdf` / `B060.md` | 红外干涉法测量外延层厚度的建模分析 | B 题(物理建模) |
| `ref/B157.pdf` / `B157.md` | 碳化硅外延层厚度的双光束和多光束干涉法测量研究 | B 题(物理建模) |
| `ref/C023.pdf` / `C023.md` | 基于混合效应模型的 NIPT 时点优化与胎儿异常判定 | C 题(数据分析) |
| `ref/C132.pdf` / `C132.md` | 基于统计建模与元学习的 NIPT 检测决策优化与异常识别 | C 题(数据分析) |

使用规则:

1. **写论文前**:浏览与本题类型相近的 1–2 篇,学习其摘要写法、章节结构、图表设计、结果呈现方式。
2. **写论文中**:遇到"这个部分怎么写"的问题(如摘要句式、过渡句、检验方法表述),回到对应论文里找范本。
3. **只学不抄**:学习结构与表达方式,绝不大段复制原文内容或公式。建模方法可以借鉴思路,但要结合本题背景重新表述。
4. **OCR 版的局限**:OCR 识别的是图片型 PDF,公式、上下标、图表可能存在识别误差。当需要精确公式或数据时,对照原 PDF(可用工具打开)或直接询问参赛者。
5. 参赛者也可以把**自己的参考论文**放进 `ref/`(或参赛文件夹的 `参考资料/`),AI Agent 会同样通读并按上述规则使用。图片型 PDF 可运行 `_tools/ocr_pdfs.py` 生成可读版(需要 Python 环境,见 `ref/README.md`)。

---

## 六、交付检查清单(写完论文后逐项核对)

**结构**
- [ ] 骨架完整:摘要 → 问题重述 → 问题分析 → 模型假设 → 符号说明 → 模型建立与求解 → 模型评价与推广 → 参考文献 → 附录
- [ ] 每个子问题都形成"分析 → 建模 → 求解 → 结果分析 → 小结"闭环
- [ ] 子问题之间有承接过渡,前问输出成为后问输入

**摘要**
- [ ] 摘要最后写,每问句式"针对…问题,首先…,建立…模型,采用…算法,得到…结果,通过…检验"
- [ ] 有具体数值与评价指标,没有"结果良好""具有一定可行性"之类的空话

**正文**
- [ ] 问题重述用自己的话,不照抄题目
- [ ] 问题分析解释了"为什么这个模型适合本题"
- [ ] 假设 5–10 条,每条有理由,后文能找到依据
- [ ] 符号三线表,全文符号统一
- [ ] 公式遵循"先文字 → 公式 → 再解释",每个公式有引导文字
- [ ] 每张图表有编号标题、正文被引用讨论、对比表最优值加粗
- [ ] 数据预处理交代了:什么问题、什么方法、关键参数、为何选、保留什么特征
- [ ] 结果可信性有检验(误差/残差/交叉验证/敏感性/多模型对比)
- [ ] 正文直接回答了题目每一问

**其他**
- [ ] 参考文献真实、格式统一、正文有引用标注
- [ ] 附录编号与正文对应
- [ ] 全文无"我们调用了 xxx 函数"这类代码说明书式表述

---

## 七、与参赛者的协作约定

1. **不清楚就问**:遇到"建模笔记缺失""结果数据对不上""题目附件打不开"等情况,先问参赛者,不擅自编造数据。
2. **结果可追溯**:论文中每个数值都要能追溯到 `结果/` 或 `代码/` 中的某个文件;找不到来源的结果不允许写进论文。
3. **先草案后终稿**:论文先出 Markdown 草稿,参赛者确认后再转 Word/LaTeX/PDF 格式。
4. **语言**:国赛默认中文;美赛需要英文版时,中文稿定稿后再翻译,并检查英文表达。

---

## 八、典型使用场景

- "帮我做这道数学建模题"(我按文件夹里的题目与笔记建模求解并写论文)
- "帮我写数模论文"(我基于建模笔记与代码结果,按本技能的骨架与规范撰写)
- "这题的建模思路怎么写"(我参考优秀论文给出分析框架)
- "帮我校对论文,检查有没有逻辑断点"
- "帮我整理参赛文件夹结构"(我按第二节的规范创建模板)
